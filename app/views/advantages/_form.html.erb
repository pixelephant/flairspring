<%= form_for(@advantage) do |f| %>
  <% if @advantage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@advantage.errors.count, "error") %> prohibited this advantage from being saved:</h2>

      <ul>
      <% @advantage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :advantage %><br />
    <%= f.text_field :advantage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
