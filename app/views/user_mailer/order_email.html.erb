<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>Köszönjük megrendelését!</h1>
    <p>Megrendelő adatai:</p>
    <ul>
      <li>Név: <%= @order.title_name + " " + @order.first_name + " " + @order.last_name %></li>
    </ul>
    <p>Számlázási cím</p>
    <%= @order.invoice_address_text %>
    <p>Szállítási cím</p>
    <%= @order.shipping_address_text %>
    <p>Termékek:</p>
    <ul>
      <% @order.order_items.each do |o_item| %>
        <%= o_item.quantity.to_s + " db " + o_item.product.name.to_s + ", ár: " + o_item.price.to_s + " Ft" %>
      <% end %>
    </ul>
  </body>
</html>