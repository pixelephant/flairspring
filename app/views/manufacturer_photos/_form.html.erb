<%= form_for(@manufacturer_photo) do |f| %>
  <% if @manufacturer_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manufacturer_photo.errors.count, "error") %> prohibited this manufacturer_photo from being saved:</h2>

      <ul>
      <% @manufacturer_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :manufacturer_id %><br />
    <%= f.number_field :manufacturer_id %>
  </div>
  <div class="field">
    <%= f.label :image_file %><br />
    <%= f.text_field :image_file %>
  </div>
  <div class="field">
    <%= f.label :alt %><br />
    <%= f.text_field :alt %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
