<%= form_for(@custom_category) do |f| %>
  <% if @custom_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@custom_category.errors.count, "error") %> prohibited this custom_category from being saved:</h2>

      <ul>
      <% @custom_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
